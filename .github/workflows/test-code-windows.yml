name: Test code windows

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  browser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Check JavaScript syntax
        run: node --check code-window-tools.js && node --check code-window-startup-render.js && node --check tests/code-window.spec.js
      - name: Install test dependencies
        run: npm install --no-audit --no-fund
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run code-window browser test
        run: npx playwright test tests/code-window.spec.js --project=desktop
      - name: Upload traces
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: code-window-test-results
          path: test-results
          if-no-files-found: warn
