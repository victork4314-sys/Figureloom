name: Disabled semantic takeover helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by export-semantic-worktree.yml on the takeover branch."
